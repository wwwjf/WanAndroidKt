<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wwwjf.hookdemo">

    <application
        android:name=".HookApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WanAndroidKt">
        <activity android:name=".HookDemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.wwwjf.hookdemo.SecondActivity"></activity>
        <!-- <activity android:name=".Hook2Activity"/> -->
        <!-- <activity android:name=".HookActivity"/> -->
        <activity android:name="com.wwwjf.hookdemo.ProxyActivity" />
    </application>

</manifest>